export declare function acquireScrollLock(owner: symbol): void;
/** Returns true when this release closed the final active overlay. */
export declare function releaseScrollLock(owner: symbol): boolean;
export declare function hasActiveScrollLock(): boolean;
